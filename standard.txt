#include <vector>
#include <string>
#include <iostream>

using namespace std;

int main(void) {

    int a =;
    double a =;
    string a =;
    vector<int> a=;
    vector<string> a=;

    int result = solution(a);
    double result = solution(a);
    string result = solution(a);

    cout << result << endl;

    vector<int> result = solution(a);
    vector<string> result = solution(a);
    
    for (int i=0; i<result.size(); ++i) {
        cout << result[i] << " ";
    } cout << endl;

    return 0;
}